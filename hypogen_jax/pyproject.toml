[project]
name = "hypogen-jax"
version = "0.1.0"
description = "JAX re-implementation of HyPoGen for cross-checking the torch results"
requires-python = ">=3.12,<3.13"
dependencies = [
    "jax[tpu]>=0.7",
    "optax>=0.2",
    "numpy>=2.0",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
